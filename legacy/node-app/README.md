# Legacy Node App

This is the original custom baby-monitor prototype.

It is archived here because the repo is now centered on Frigate deployment rather
than maintaining a separate custom frontend as the primary product.

Keep this code only if we later want custom features that Frigate does not cover
well enough, such as family-specific summaries or inferred household events.
